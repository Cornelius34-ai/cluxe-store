// src/lib/supabase/queries.ts
// Empty for now - we will add queries once we have data and schema
export const getCategories = async () => {
  // Placeholder
  return [];
};

export const getProducts = async () => {
  // Placeholder
  return [];
};

export const getProductBySlug = async (slug: string) => {
  // Placeholder
  return null;
};